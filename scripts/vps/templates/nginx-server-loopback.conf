
# Loopback-only server for @@APP@@, used by deploy.sh / smoke.sh health checks (never reachable from outside).
server {
    listen 127.0.0.1:@@LOOPBACK_PORT@@;
    server_name localhost;
    include @@NGINX_ETC@@/snippets/r007-@@APP@@-body.conf;
}
